# Optional: browser calls API directly. Leave unset to use same-origin /api/proxy → BACKEND_URL (default http://127.0.0.1:8000).
# Docker Compose often sets: NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
