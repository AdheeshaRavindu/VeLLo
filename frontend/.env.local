# Leave NEXT_PUBLIC_API_BASE_URL unset so the app uses /api/proxy (same origin → BACKEND_URL).
# Uncomment only if the browser must talk to the API directly:
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
